const getRandomPosition = () => ({
    top: Math.random() * 90 + '%',
    left: Math.random() * 90 + '%',
});

export default function Box() {
    
    return (
        <>
            
        </>
    );
};